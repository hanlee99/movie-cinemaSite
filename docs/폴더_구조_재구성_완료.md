# ✅ 폴더 구조 재구성 완료

## 📁 새로운 폴더 구조

### templates/
```
templates/
├── layout/              # 레이아웃 템플릿
│   ├── default.html
│   ├── admin-layout.html
│   ├── auth-layout.html
│   └── user-layout.html
├── fragments/           # 공통 Fragment
│   ├── components/
│   │   ├── header.html
│   │   ├── footer.html
│   │   └── sidebar.html
│   └── sections/
│       ├── boxoffice.html
│       ├── cinema.html
│       └── map.html
├── pages/               # 일반 페이지 (새로 생성)
│   ├── home.html
│   ├── detail.html
│   ├── search.html
│   └── cinema-list.html
├── admin/               # 관리자 페이지
│   ├── dashboard.html
│   └── sync.html
├── user/                # 사용자 페이지
│   ├── login.html
│   ├── mypage.html
│   ├── register.html
│   └── watch-history.html
├── auth/                # 인증 페이지 (새로 생성)
│   └── login.html
└── error/               # 에러 페이지
    ├── 403.html
    └── 404.html
```

### static/css/
```
static/css/
├── base/                # 기본 스타일 (새로 생성)
│   └── main.css
├── components/          # 컴포넌트 스타일 (새로 생성)
├── layouts/            # 레이아웃별 스타일 (새로 생성)
└── pages/               # 페이지별 스타일 (sections → pages로 변경)
    ├── boxoffice.css
    ├── cinema.css
    └── showtime.css
```

### static/js/
```
static/js/
├── base/                # 기본 스크립트 (새로 생성)
│   └── app.js
├── components/          # 컴포넌트 스크립트 (새로 생성)
├── layouts/             # 레이아웃별 스크립트 (새로 생성)
└── pages/               # 페이지별 스크립트 (sections → pages로 변경)
    ├── boxoffice.js
    ├── cinema.js
    ├── cinema-list.js
    └── map.js
```

---

## 🔄 변경 사항

### 1. Templates 구조
- ✅ `pages/` 폴더 생성 → 일반 페이지 이동
  - `home.html` → `pages/home.html`
  - `detail.html` → `pages/detail.html`
  - `search.html` → `pages/search.html`
  - `cinema-list.html` → `pages/cinema-list.html`
- ✅ `auth/` 폴더 생성 → 인증 페이지 이동
  - `login.html` → `auth/login.html`

### 2. CSS 구조
- ✅ `base/` 폴더 생성
  - `main.css` → `base/main.css`
- ✅ `pages/` 폴더 생성 (기존 `sections/` 대체)
  - `sections/boxoffice.css` → `pages/boxoffice.css`
  - `sections/cinema.css` → `pages/cinema.css`
  - `sections/showtime.css` → `pages/showtime.css`

### 3. JS 구조
- ✅ `base/` 폴더 생성
  - `app.js` → `base/app.js`
- ✅ `pages/` 폴더 생성 (기존 `sections/` 대체)
  - `sections/boxoffice.js` → `pages/boxoffice.js`
  - `sections/cinema.js` → `pages/cinema.js`
  - `sections/map.js` → `pages/map.js`
  - `cinema-list.js` → `pages/cinema-list.js`

---

## 📝 경로 업데이트

### 레이아웃 파일
- ✅ `layout/default.html`: CSS/JS 경로 업데이트
  - `/css/main.css` → `/css/base/main.css`
  - `/js/app.js` → `/js/base/app.js`

### 페이지 파일
- ✅ `pages/home.html`: CSS 경로 업데이트
  - `/css/sections/*.css` → `/css/pages/*.css`
- ✅ `pages/cinema-list.html`: CSS/JS 경로 업데이트
  - `/css/sections/cinema.css` → `/css/pages/cinema.css`
  - `/js/cinema-list.js` → `/js/pages/cinema-list.js`

### JavaScript import 경로
- ✅ `base/app.js`: import 경로 업데이트
  - `./sections/boxoffice.js` → `../pages/boxoffice.js`

---

## ⚠️ 주의사항

### 1. 컨트롤러 경로 확인 필요
페이지가 `pages/` 폴더로 이동했으므로, 컨트롤러에서 반환하는 템플릿 경로를 확인해야 합니다:

```java
// 기존
return "home";  // templates/home.html

// 변경 후
return "pages/home";  // templates/pages/home.html
```

또는 Thymeleaf 설정에서 prefix를 확인해야 할 수 있습니다.

### 2. 레이아웃 내부 CSS/JS
레이아웃 파일(`layout/*.html`) 내부에 인라인으로 작성된 CSS/JS는 그대로 유지됩니다.
- `admin-layout.html`: 사이드바 스타일 (인라인)
- `auth-layout.html`: 인증 페이지 스타일 (인라인)
- `user-layout.html`: 사용자 페이지 스타일 (인라인)
- `default.html`: Toast 알림 스크립트 (인라인)

이것은 의도된 것이며, 레이아웃별로 특화된 스타일/스크립트이므로 문제없습니다.

---

## 🎯 다음 단계

### 1. 컨트롤러 경로 업데이트
```java
// HomeController.java
@GetMapping("/")
public String home(Model model) {
    // ...
    return "pages/home";  // 변경 필요
}

@GetMapping("/movies/{id}")
public String movieDetail(@PathVariable Long id, Model model) {
    // ...
    return "pages/detail";  // 변경 필요
}

@GetMapping("/search")
public String search(...) {
    // ...
    return "pages/search";  // 변경 필요
}

@GetMapping("/cinema/list")
public String cinemaList() {
    return "pages/cinema-list";  // 변경 필요
}
```

### 2. 기존 파일 정리 (선택사항)
- `templates/` 루트의 `home.html`, `detail.html` 등 (이미 이동됨)
- `static/css/sections/` 폴더 (비어있으면 삭제 가능)
- `static/js/sections/` 폴더 (비어있으면 삭제 가능)

---

## ✅ 완료된 작업

1. ✅ 폴더 구조 재구성
2. ✅ 파일 이동
3. ✅ 경로 업데이트
4. ✅ 레이아웃 내부 CSS/JS 유지 (의도된 대로)

---

**작성일**: 2025-01-XX  
**버전**: 1.0  
**상태**: ✅ 폴더 구조 재구성 완료


